<Window x:Class="Good_Teacher.Windows.Special.Window_TextEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Good_Teacher.Windows.Special"
        mc:Ignorable="d"
        xmlns:strings="clr-namespace:Good_Teacher.Strings"
        Title="{x:Static strings:ResStrings.TextEditor}" MinHeight="300" MinWidth="400" Height="450" Width="600" Icon="/Good Teacher;component/Resources/Icons/Buttons/Write.png" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox TextWrapping="Wrap" AcceptsReturn="true" Grid.Row="0" x:Name="TB_TextEditor" FontSize="14" />

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <Button Click="ButtonOK_Click" Content="{x:Static strings:ResStrings.OK}" Margin="5" Padding="20,5" />
        </StackPanel>

    </Grid>
</Window>
